# Tested dependency versions for bit-faithful reproduction of the paper's numbers.
# pyproject.toml uses flexible (>=) ranges for installability; this file pins the
# EXACT versions the committed results were produced with (see results/MANIFEST.json,
# docs/REPRODUCE.md). Python 3.13.
#
# GPU runs used torch 2.6.0+cu124 (CUDA 12.4). For a CPU-only repro of the fast tests
# and the figure pipeline, install the CPU torch wheel instead:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
#
# Install:  pip install -r requirements-lock.txt  (then: pip install -e .)

numpy==2.4.6
scipy==1.17.1
torch==2.6.0+cu124
matplotlib==3.11.0
networkx==3.6.1
pyvista==0.48.4
PyYAML==6.0.3
tqdm==4.68.2
airfrans==0.1.5.1
