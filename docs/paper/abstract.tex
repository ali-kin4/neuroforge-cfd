% Shared abstract prose. Included by BOTH neuroforge_cfd.tex (TMLR) and
% neuroforge_cfd_elsevier.tex (elsarticle) so the two cannot drift.
% Kept to the 250-word limit of the Journal of Computational Science guide; the
% Keywords line is a separate Editorial Manager field and is not counted.
% The word count is measured by scripts/check_submission.py (strip_tex
% semantics: bare numerals count as words, a bare "%" does not). The script is
% the authority; run it after any edit here, because the margin is small.
%
% The covariate-null and `reynolds`-split sentences were removed rather than
% shortened: that material is the spine of a separate paper and must not be
% journal-published (docs/paper/sections/covariate_null_removed.tex).
%
% FOUR SCOPE POINTS ARE DELIBERATE AND MUST NOT BE COMPRESSED AWAY.
% (1) Two denominators, never mixed. The REGISTERED verdicts are read against
%     Transolver's band error pooled over all 200 cases and all band nodes, as
%     registered: 25x (amendment 3) and 0.0044x (B1). Every per-case figure is
%     PAIRED -- each case against its own Transolver error on identical nodes
%     (scripts/paired_band_stats.py): "worse than the surrogate on each of the
%     200 cases" (paired physical bound, none <= 1), "below the surrogate on every
%     case" (paired body-fitted bound, 200/200 < 1), "1.7x" (deployed estimator,
%     case-mean on matched nodes). Earlier drafts quoted 161/200, 152/200 and a
%     median of 0.38x against the pooled constant; those are retired.
% (2) The abstract claims the BOUND and never the estimator. The deployed
%     estimator in the body-fitted frame wins at the paired median (0.83x, 115/200
%     cases) and LOSES on the case-mean (1.7x). Do not let the sentence drift into
%     "parameter interpolation beats the surrogate": the claim is that no member
%     of the family is BARRED from the wall by representation.
% (3) Pressure was computed in the same run but no rule was registered for it:
%     the abstract says so. "worse on 185 of 200 cases" is the paired count
%     (15/200 better); the case-mean is 1.58x worse, the paired median 6.1x.
% (4) The bound was run on u and p only (ls_chans). The title names the family
%     and does not claim "velocity", because v and nu_t were never bounded.
%
% RETIRED 2026-09-16 by amendment 3, kept for history: the least-squares family
% bound was once a THREE-CASE probe returning 21.3x. It is now 200 cases and
% 25.0x. The 344x oracle was removed from the abstract entirely on 2026-09-18 --
% it is a minimum over SINGLE fields and is not a bound on their combinations,
% so leading with it invited exactly the overclaim the bound avoids.
Machine-learning flow surrogates are almost universally scored by resampling their
predictions onto a uniform grid. We measure what it costs, then what it hides. On a
standard external-aerodynamics benchmark, kernel interpolation over seven scalars from the
case name---no network, no learning---gives $8.4\times$ lower volume-pressure error than a
Transolver trained on the same data on an area-uniform $128^2$ raster. Re-weighting those
identical predictions by the node measure gives $0.44\times$, and scoring per node on the
native cloud $0.21\times$: three measures in current use, and the ranking reverses. At
native resolution the interpolator loses on \textbf{every} channel, and inside the first
$0.005$ chord the exact least-squares bound---what \emph{any} weighting of the $800$
training fields can achieve---is $25\times$ worse on average, and worse on all $200$ cases.
That impossibility result holds only in the frame it is posed in. There a query lands
$7.8\times$ further from the training wall than from its own, with $35\%$ of the kernel
weight \emph{inside} the training airfoil; re-posed in a parameter-free wall-following
frame, nothing retuned, the same bound falls to $\mathbf{0.0044\times}$, below the
surrogate on every case. We claim the \emph{barrier}, not the gap: the deployed estimator
improves $112\times$ but still trails on the case-mean ($1.7\times$). On pressure, where no
rule was registered, there was never a barrier, and the frame worsens the bound on $185$ of
$200$ cases. The frame is undefined on $5\%$ of near-wall nodes, and the raster's own
round-trip error at the nodes exceeds the surrogate's by $413\times$.
