% Shared abstract prose. Included by BOTH neuroforge_cfd.tex (TMLR) and
% neuroforge_cfd_elsevier.tex (elsarticle) so the two cannot drift.
% Kept to the 250-word limit of the Journal of Computational Science guide; the
% Keywords line is a separate Editorial Manager field and is not counted.
% Hand count under scripts/check_submission.py's strip_tex semantics after the
% 2026-09-12 JOCS rebuild: ~245 words. Bare numerals count as words; a bare "%"
% does not; "128^2" and "channel---144" are one token each. VERIFY with
% scripts/check_submission.py before filing -- the hand count is an estimate,
% the script is the authority, and the margin here is only a few words.
%
% The covariate-null and `reynolds`-split sentences were removed rather than
% shortened: that material is the spine of a separate paper and must not be
% journal-published (docs/paper/sections/covariate_null_removed.tex).
%
% THREE SCOPE POINTS ARE DELIBERATE AND MUST NOT BE COMPRESSED AWAY.
% (1) The band separation is monotone on the GRID ladder (D2 CONFIRMED, 0
%     inversions) and NOT monotone at the native nodes (P2 PARTIAL, 2
%     inversions, the far-field u entry reversing). The abstract therefore
%     claims agreement in SIZE and names the one disagreement, and the word
%     "monotonically" is not used.
% (2) "0.0044x, better on 200/200 cases" is the BOUND -- the least-squares
%     projection, which is unanimous and has no tail. The DEPLOYED estimator in
%     the same frame wins at the median (0.38x) and on 152/200 cases but LOSES
%     on the case-mean (1.556x), because a minority of cases carry a heavy tail.
%     The abstract claims the bound and never the estimator. Do not let the
%     sentence drift into "parameter interpolation beats the surrogate": the
%     claim is that no member of the family is BARRED from the wall by
%     representation. sec:interp states both statistics side by side.
% (3) "the physical arm on those same nodes still reads 25.2x" is the
%     exclusion-innocence number and it is load-bearing, not colour. The frame
%     is undefined on 4.98% of near-wall nodes and they are dropped from BOTH
%     arms; 25.2 against 25.0 unrestricted is what shows the drop manufactured
%     nothing. If the sentence is ever cut for length, the result becomes
%     attackable in one line.
%
% RETIRED 2026-09-16 by amendment 3, kept for history: the least-squares family
% bound was once a THREE-CASE probe returning 21.3x. It is now 200 cases and
% 25.0x. The 344x oracle was removed from the abstract entirely on 2026-09-18 --
% it is a minimum over SINGLE fields and is not a bound on their combinations,
% so leading with it invited exactly the overclaim the bound avoids.
Machine-learning surrogates for simulation are almost universally scored by resampling
their predictions onto a uniform grid. We measure what it costs, then what it hides. On a
standard external-aerodynamics benchmark, kernel interpolation over seven scalars from the
case name---no network, no learning---gives $8.4\times$ lower volume-pressure error than a
Transolver trained on the same data on an area-uniform $128^2$ raster. Re-weighting those
identical predictions by the node measure gives $0.44\times$, and scoring per node on the
native cloud $0.21\times$: three measures in current use, and the ranking reverses. At
native resolution the interpolator loses on \textbf{every} channel, and inside the first
$0.005$ chord the exact least-squares bound---what \emph{any} weighting of the $800$
training fields can achieve---is $25\times$ worse on all $200$ cases. That is an
impossibility result, and it holds only in the frame it is posed in. A query there lands
$7.8\times$ further from the training case's wall than from its own, with $35\%$ of the
kernel weight \emph{inside} the training airfoil; re-posed in a parameter-free
wall-following frame, nothing retuned, the same bound falls to $\mathbf{0.0044\times}$ on
$\mathbf{200/200}$ cases. We claim the \emph{barrier}, not the gap: the deployed estimator
improves $114\times$ and still loses on the case-mean. The effect is channel-selective as
the mechanism predicts---on pressure, nearly constant across a boundary layer, there was
never a barrier and the frame costs $1.6\times$. That frame is undefined on $5.0\%$ of
near-wall nodes, and the raster's own round-trip error there exceeds the surrogate's by
$413\times$.
